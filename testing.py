import Scrabble, bruteforcer
import unittest
from string import ascii_uppercase

def strings_to_board(letter_placements):
	game = Scrabble.Scrabble()
	for r_ind, r_val in enumerate(letter_placements):
		for c_ind, c_val in enumerate(r_val):
			if c_val in ascii_uppercase:
				new_tile = Scrabble.Tile(board.
				board[r, c] 


